# Database Configuration
R2DBC_URL=r2dbc:postgresql://localhost:5432/chillflix
R2DBC_USERNAME=postgres
R2DBC_PASSWORD=postgres

# Server Configuration
SERVER_PORT=8080

# API Credentials (replace with your actual credentials)
TMDB_API_KEY=your_tmdb_api_key

# Docker Hub Credentials (for local deployments)
DOCKER_HUB_USERNAME=your_dockerhub_username
DOCKER_HUB_TOKEN=your_dockerhub_token

# Security Scan Credentials (for local security scans)
SNYK_TOKEN=your_snyk_token
SONAR_TOKEN=your_sonar_token
EOF < /dev/null